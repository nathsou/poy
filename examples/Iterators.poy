import Foundations { print }

#as("boxgirl")
let boxboy = 2019

print(boxboy)

extend<a> a[] {
    declare {
        let length: Num
        fun at(index: Num): a
        // #as("Symbol.iterator") fun iter(): Iterator<a>
    }

    fun iter() {
        mut i = 0

        while i < self.length {
            yield self.at(i)
            i += 1
        }
    }
}

for n in [1, 2, 3] {
    print(n)
}
